parameters:
  build_type:
    type: enum
    default: release
    enum: [release, staging]
docker:
  - image: cimg/android:2024.01.1-node
resource_class: large
shell: /bin/bash -eo pipefail
environment:
  GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m" -Dorg.gradle.daemon=false -Dorg.gradle.workers.max=2'
  FASTLANE_SKIP_UPDATE_CHECK: true
steps:
  - checkout
  - prepare_workspace
  - restore_environment_variables
  - install_flutter:
      precache: android
  - prepare_project:
      build_type: << parameters.build_type >>
  - install_fastlane:
      directory: android
  - install_gradle_dependencies
  - run:
      # The following command is necessary since Ninja is required to build the app for android but is not included in cimg/android
      # https://github.com/CircleCI-Public/cimg-android/issues/129
      name: Install Ninja
      command: sudo apt-get update && sudo apt-get install -y ninja-build
  - run:
      name: '[FL] Prepare Android Keystore'
      command: bundle exec fastlane android keystore
      working_directory: android
  - run:
      name: '[FL] Build'
      command: bundle exec fastlane android build version_name:${NEW_VERSION_NAME} version_code:${NEW_VERSION_CODE} build_type:<< parameters.build_type >>
      working_directory: android
  - run:
      name: Move aab
      command: mv ~/project/build/app/outputs/bundle/<< parameters.build_type >>/app-<< parameters.build_type >>.aab app-<< parameters.build_type >>.aab
  - store_artifacts:
      path: app-<< parameters.build_type >>.aab
  - run:
      name: Move apk
      command: mv ~/project/build/app/outputs/apk/<< parameters.build_type >>/app-<< parameters.build_type >>.apk app-<< parameters.build_type >>.apk
  - store_artifacts:
      path: app-<< parameters.build_type >>.apk
  - persist_to_workspace:
      root: .
      paths:
        - app-<< parameters.build_type >>.aab
        - app-<< parameters.build_type >>.apk
  - persist_environment_variables
